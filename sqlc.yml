version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "db"
        out: "lib/db"
        sql_package: "pgx/v5"
    rules:
      - sqlc/db-prepare
    analyzer:
      database: false
